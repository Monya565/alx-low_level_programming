#include "main.h"
/**
 * print_numbers - prints numbers.
 *
 * Return: void.
 */
void print_numbers(void)
{
char a;
for (a >= '0'; a <= '9'; a++)
{
if (!(c == '2' || c == '4'))
_putchar(a);
}
_putchar('/n');
}
