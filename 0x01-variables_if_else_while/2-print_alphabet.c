#include <stdio.h>
/**
 * main - Prints any alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
char letter, letter1;
for (letter = 'a'; letter <= 'z'; letter++)
putchar(letter);
putchar('\n');
return (0);
}
