#include "main.h"
/**
 * print_alphabet - prints the alphabet in lowercase, followed by a new line.
 * Return : 0.
 */
int main(void)
{
char let;
  for (let = 'a'; let <= 'z'; let++)
    _putchar(let);
  _puchar(\n);
  return 0;
}
